import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:localreview/features/home/presentation/view/home_page.dart';

void main() {
  // testWidgets('HomePage navigation test', (WidgetTester tester) async {
  //   // Build the HomePage widget.
  //   await tester.pumpWidget(
  //     MaterialApp(
  //       home: HomePage(),
  //     ),
  //   );

  //   // Verify the initial page is FeedPage.
  //   expect(find.text('Feed'), findsOneWidget);

  //   // Tap on the "Friends" tab in the bottom navigation bar.
  //   await tester.tap(find.byIcon(Icons.people));
  //   await tester.pump();

  //   // Verify that the selected page is now "Friends".
  //   expect(find.text('Friends'), findsOneWidget);

  //   // Tap on the "Chat" tab in the bottom navigation bar.
  //   await tester.tap(find.byIcon(Icons.chat));
  //   await tester.pump();

  //   // Verify that the selected page is now "Chat".
  //   expect(find.text('Chat'), findsOneWidget);

  //   // Tap on the "Profile" tab in the bottom navigation bar.
  //   await tester.tap(find.byIcon(Icons.person));
  //   await tester.pump();

  //   // Verify that the selected page is now "Profile".
  //   expect(find.text('Profile'), findsOneWidget);
  // });
}
